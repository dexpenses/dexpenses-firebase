{
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "functions": {
    "predeploy": [
      "npm --prefix \"$RESOURCE_DIR\" run test -- --silent",
      "npm --prefix \"$RESOURCE_DIR\" run build"
    ]
  },
  "storage": [
    {
      "target": "default",
      "rules": "storage.rules"
    },
    {
      "target": "public",
      "rules": "storage.public.rules"
    },
    {
      "target": "test-images",
      "rules": "storage.test-images.rules"
    }
  ]
}
